from setuptools import setup

setup(name="pgscript", version="0.1", description="high level objects for pygame", author="Ashwin Rajesh", packages=['pgscript'], zip_safe=False, install_requires=['pygame'])
